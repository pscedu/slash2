# $Id$

include Makefile.path

PROG=		slimmns_format
SRCS+=		slimmns_format.c
SRCS+=		wipedir.c
SRCS+=		${SLASH_BASE}/share/str.c
SRCS+=		${PFL_BASE}/psc_util/alloc.c
SRCS+=		${PFL_BASE}/psc_util/init.c
SRCS+=		${PFL_BASE}/psc_util/log.c

include ${SLASHMK}
